====================================================================
Q: 016 现在什么状态？这个预警结果可信吗？
--------------------------------------------------------------------
## 016 当前状态

**D(t) 三级语义（016）**

| 级别 | 含义 | 触发寿命位置 |
|---|---|---|
| L1 | D≥0.25 检测起始 | 45.9% |
| L2 | D≥0.55 不可逆确认 | 59.9% |
| L3 | D≥0.85 已无裕度 | 95.8% |

预警判据为「D 首次不可逆 ≥0.3（2% 寿命窗内不回落 0.15）」，因此 **t_warn = 46.9% 寿命**，落在 L1 与 L2 之间 —— 即：**016 已越过检测起始、且已进入不可逆确认阶段；到 L3（95.8%）前仍有约 36% 寿命的裕度区间。**工具给出的评级为 A 合理：相对扩展标签 b2=43.8%，偏差 3.1%，距断裂锚（b3=99%）提前 52.1%。

## 可信度：位置可信，但有两个口径限制

**支持可信的独立证据（刚度，非标签口径）**

- 刚度损失 L≥5% 出现在 **49.0%** 寿命，与 D 的检测起始 45.9%/t_warn 46.9% 相差约 2–3% 寿命；
- L≥10% 出现在 **59.0%** 寿命，与 D 的不可逆确认 L2=59.9% 仅差 **0.9%** 寿命。

两个互相独立的口径（D 的多源判据 vs 循环幅值刚度退化）在同一试件上几乎重合，说明 **016 的预警时刻不是标签拟合出来的**，时间位置是可信的。文档也印证：D vs 仅 AE 的 p=1.00，016 这类样本的 |err| 均值 CI 为 [2.8, 3.2]，016 的 3.1% 属典型水平。

**必须提示的限制**

1. **“偏差 3.1% / 提前 52.1%”不是物理真值精度。** 工具明确说明 b2 是**扩展标签**，非独立物理真值；提前量本质是 b3(99%) − t_warn，属口径内算术。真正的外部参照是上一条的刚度。
2. **016 的应变是解调口径（DEMOD），是主样本中唯一的例外。** 加之各试件贴片位置不同、校准段基线差异极大（5.74/11.76/17.43/16.76/36.17），因此所有应变量只能用相对自身基线的变化，**016 的 D 绝对值不应与其他试件并列比较**，只有时刻/趋势可比。
3. **AE 时间轴是“猜”出来的。** 016 的 AE 无时间戳，靠 `np.linspace` 均匀映射；且 016 为事件驱动型、等效频率 **6.27 Hz**，与应变采样 **10.0 Hz** 不匹配（对比 018–020 为 1.00 Hz 固定窗口、与应变严格 1:1）。口径不一致时时间轴会失真 —— 所以 016 中 **AE 与应变的对齐精度弱于 018–020**，D(t) 的秒级/块级时刻不宜当作硬结论。
4. **采集连续性本身没有问题**：时长 1.39 h，dt 中位与最大均为 0.1 s（10 Hz 均匀），无缺口；AE 计数 31442 条。
5. **一个分辨率疑点**：刚度阈值 L≥30% 与 L≥50% 都落在 **87%** 寿命同一点，说明末段幅值是跳变式跨过（100 块 → 1% 寿命分辨率）。因此 87% 这个点不可当精确时刻；末值 stiff_end=2.574 低于 stiff_max=3.758，末段幅值有回落，刚度末值同样不宜跨试件绝对值比较。

## 结论

- **状态**：016 已完成检测起始与不可逆确认（L2 在 59.9%），尚未达「无裕度」（L3 = 95.8%）。
- **可信度**：**预警时刻/量级可信**（被刚度这一独立物理参照在 2% 寿命内双重印证）；但 **D 的应变分量口径（DEMOD）与 AE 时间轴映射是本样本独有的短板**，跨试件绝对值和秒级时刻都不可当结论使用。若要做跨试件横向排名，建议只比时刻、并单独标注 016 的应变口径特殊性与 018–020 的固定窗口口径差异。

需要的话，我可以把 016 与 017–020 做一次横向对比（预警时刻/提前量/刚度末值），看 016 是否在上述限制下仍是组内一致的表现。
--------------------------------------------------------------------
[2 轮 / 24.0 s / tokens in=8150 out=4463]  规则检查=72 拦截=0

--- 推理轨迹 ---
  warning_status({"gid": "016"}) -> OK
  stiffness({"gid": "016"}) -> OK
  data_quality({"gid": "016"}) -> OK
  doc_search({"query": "解调口径 DEMOD 应变"}) -> OK
  doc_search({"query": "AE 时间戳 均匀映射 等效频率"}) -> OK